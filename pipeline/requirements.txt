# TWI pipeline dependencies. Install into a venv:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# WhiteboxTools downloads its native binary automatically on first run.
whitebox>=2.3.1
rasterio>=1.3
numpy>=1.24
matplotlib>=3.7
pillow>=10.0
requests>=2.31
flask>=3.0
shapely>=2.0
